利用Vector代替数组处理，从键盘读取学生成绩（以负数代表输入结束），找出最高分，并输出学生的等级。
提示：数组一旦创建，长度就固定不变，所以在创建数组前就需要知道数组的长度。而向量java.lang.Vector可以根据需要

1.创建Vector对象：Vector v = new Vector();
2.给向量添加元素：v.addElement(Object obj);
3.取出向量中的元素：